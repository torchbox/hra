apiVersion: batch/v2alpha1
kind: CronJob
metadata:
  name: {{ .Release.Name }}-publish-scheduled-pages
spec:
  schedule: "*/5 * * * *"
  concurrencyPolicy: Forbid
  successfulJobsHistoryLimit: 1
  failedJobsHistoryLimit: 1
  jobTemplate:
    spec:
      template:
        spec:
          restartPolicy: Never
          containers:
              - name: hra
                image: {{ required "A valid dockerRegistry is required" .Values.DockerRegistry }}/hra
                command:
                - /sbin/k8s-safe-cronjob
                {{ if .Values.RavenDsn }}
                - "-dsn"
                - {{ .Values.RavenDsn }}
                {{ end }}
                - python
                - manage.py
                - publish_scheduled_pages_blork
                {{- include "environment" . | indent 16 }}
